Geogram project example
=======================

Compile and run as usual (geogram is downloaded automatically by CMake):

```
mkdir build
cd build
cmake ..
make -j 8
```

Run as follows:

```
./voronoi bunny.obj
```
